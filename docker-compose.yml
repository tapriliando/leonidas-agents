# Phase 0: Redis placeholder for Phase 7 caching; add backend/mcp services when wired.
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
